将任务间依赖的DAG框架化，实现更为通用的模式，即给出任务DAG，对于任意Stage，都可以自动进行判断执行顺序


stage在put_data处接到任务开始通知，在worker处接到任务完成通知，以此控制任务依赖
需要scheduler，根据DAG顺理任务顺序




每个库安装的顺序执行次序:
下载和安装依赖，配置，构建，安装

每步信息填入:
(name, version, compiler, platform, arch, build_type)
下载和安装依赖

(name, version, compiler, platform, arch, build_type, 
install_path, others)
配置

(name, version, compiler, platform, arch, build_type, 
install_path, others)
构建

(name, version, compiler, platform, arch, build_type, 
install_path, others)
安装






任何一步发生错误，都应当尝试是否能自动修复，如果不能则尝试让用户手动修复，否则安装失败，输出错误信息