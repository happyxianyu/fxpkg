
package api修改
安装数据查询，''可以匹配任何值
promise/future的executor



每个库安装的顺序执行次序:
下载和安装依赖，配置，构建，安装

每步信息填入:
(name, version, compiler, platform, arch, build_type)
下载和安装依赖

(name, version, compiler, platform, arch, build_type, 
install_path, others)
配置

(name, version, compiler, platform, arch, build_type, 
install_path, others)
构建

(name, version, compiler, platform, arch, build_type, 
install_path, others)
安装






任何一步发生错误，都应当尝试是否能自动修复，如果不能则尝试让用户手动修复，否则安装失败，输出错误信息



None表示任意
''表示空